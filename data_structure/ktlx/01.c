// 创建一个顺序表，并从键盘接收数字输入，将输入的正整数按从小到大的顺序插入顺序表，
// 并在输入负整数的时候将其绝对值数据删除。每次输入后，将顺序表的内容打印到屏幕上。
#include <stdio.h>

typedef struct
{
    int cap;
    int last;
    int *data;
}list;

list * init(int cap){
list * head = mallic(sizeof(list));
if (head != NULL)
{
    
}


}

int main(int argc, char const *argv[])
{
    int cap;
    init(cap);
    return 0;
}
